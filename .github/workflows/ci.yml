name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Check formatting
        if: runner.os == 'Linux'
        run: test -z "$(gofmt -l .)"
      - name: Vet
        run: go vet ./...
      - name: Test
        run: go test ./...
      - name: Build
        # .exe suffix everywhere: Windows needs it to execute the file,
        # unix doesn't care
        run: go build -o csm-bin.exe ./cmd/csm
      - name: Smoke test (no claude binary needed)
        shell: pwsh
        env:
          CSM_HOME: ${{ runner.temp }}/csm-home
        run: |
          $ErrorActionPreference = "Stop"
          $PSNativeCommandUseErrorActionPreference = $true
          ./csm-bin.exe --version
          ./csm-bin.exe init
          ./csm-bin.exe add "Smoke test task" --effort low --mode plan --priority 3
          ./csm-bin.exe add "Second task"
          ./csm-bin.exe list
          ./csm-bin.exe config set default_effort high
          if ((./csm-bin.exe config get default_effort) -ne '"high"') { throw "config set/get mismatch" }
          ./csm-bin.exe config unset default_effort
          ./csm-bin.exe status
          ./csm-bin.exe run --dry-run
          ./csm-bin.exe clear --all
          ./csm-bin.exe list
